I attempted to modify Proyecto_Veterinaria.csproj but IDE prevented edits because solution is open. I updated agregar.cs to use embedded resources (Properties.Resources.gato_png, perro_png) when available and LoadImageSafe otherwise. Next step: user asked to close solution; once closed I can update .csproj to mark Resources files as Content and CopyToOutputDirectory. Alternatively, developer can set properties in Visual Studio as instructed.